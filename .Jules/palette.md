## 2024-05-22 - Inputs in Drawers need Explicit Labels
**Learning:** Inputs inside custom UI drawers often lack visual labels due to space constraints or design, making them inaccessible.
**Action:** Always verify accessible names for inputs in "slide-out" or "modal" UI patterns using `aria-label` if `<label>` is not visually appropriate.
