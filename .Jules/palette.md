## 2024-10-24 - Missing Labels on Hidden Panels
**Learning:** Inputs inside toggleable panels (like "Share/Import") often get missed during initial accessibility sweeps because they aren't visible by default.
**Action:** When auditing forms, always toggle all UI states (drawers, modals, panels) to find hidden inputs that lack accessible names.
